{% extends "::base8.html.twig" %}
{% block body %}
<form action="{{ path('contactos_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('contactos') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock body %}