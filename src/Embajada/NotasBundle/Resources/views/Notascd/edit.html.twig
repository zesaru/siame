<html>
<head>  
  <style>
  #contenedor{
         
        margin-left: 100px;  
        width: 1000px; 
                      
                     
    }
    #izquierda{
        float: left;
        margin:auto;  
        width: 300px; 
                     
                      
    }
    #derecha{
        float: right;
        margin:auto;  
        width: 300px; 
                      
                      
    }
    #piedepagina{
        position:absolute;
        top:600;
        right: 0;
        width: 300px;
        
        }
    </style>

</head>
<h1>Notas Cuerpo Diplomatico</h1>

{#<form action="{{ path('notascd_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>#}

<body>

{% if entity.aprobado==0 %} 
<h1> Editar Notas</h1>
<div id="contenedor">
<form action="{{ path('notascd_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_errors(edit_form) }}
    <div id="izquierda">
    {{form_row(edit_form.idioma)}}    

        <div>
         {{ form_errors (edit_form.sumilla) }} 
         {{ form_label (edit_form.sumilla) }}  
         {{ form_widget (edit_form.sumilla, { 'attr': {
        'size'      :60,
        }}
         )}}

        </div> 
        <div>
            {{ form_errors(edit_form.cuerpo) }}
            {{ form_widget (edit_form.cuerpo, { 'attr': {
            'cols'  :70,
            'rows'  :30
            }}

            )}}
        </div>        
    </div>
    <div id="derecha">
        <div>
            {{ form_label(edit_form.destinatario) }}
            {{ form_errors(edit_form.destinatario) }}
            {{ form_widget (edit_form.destinatario, { 'attr': {
            'cols'  :50,
            'rows'  :5
            }}

            )}}
        </div>
        <div>
            {{ form_label(edit_form.observaciones) }}
            {{ form_errors(edit_form.observaciones) }}
            {{ form_widget (edit_form.observaciones, { 'attr': {
            'cols'  :50,
            'rows'  :5
            }}

            )}}
        </div>
        <div>
            {{ form_label(edit_form.aprobado, 'Aprobado') }}
            {{ form_errors(edit_form.aprobado) }}
            {% if is_granted("ROLE_ADMIN") %}
                            
               {{ form_widget(edit_form.aprobado) }}
             
            {% else %} 
                {{ form_widget(edit_form.aprobado, { 'attr': {
                    'disabled':true,}}
                ) }}
            {% endif %}    
            
        </div> 
        <div>
              {{ form_rest(edit_form)}}
        </div>
    <div>      
    <p>
        <button type="submit">Editar</button>
    </p>

</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('notascd') }}">
            Back to the list
        </a>
    </li>

</ul>
</div>          
{% else %}
Esta Nota Dipolomatica no se puede modificar

{% endif%}
{#<form action="{{ path('notascd_delete', { 'id': entity.id }) }}" method="post">#}
